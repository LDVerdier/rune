services:
  - type: web
    name: rune
    runtime: docker
    plan: starter
    repo: https://github.com/LDVerdier/rune
    branch: main
    autoDeploy: false
    envVars:
      - key: NODE_ENV
        value: production
