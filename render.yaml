services:
  - type: web
    name: rune
    runtime: docker
    plan: free
    repo: https://github.com/laurentmusic/rune
    branch: main
    autoDeploy: false
    envVars:
      - key: NODE_ENV
        value: production
