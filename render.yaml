services:
  - type: web
    name: rune
    runtime: docker
    plan: free
    repo: https://github.com/LDVerdier/rune
    branch: main
    autoDeploy: false
    envVars:
      - key: NODE_ENV
        value: production
